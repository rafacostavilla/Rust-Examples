fn main() {
    let spaces = "     ";
    println!("Spaces ->{spaces}<- Spaces");

    let spaces = spaces.len();
    println!("Number of spaces: {spaces}");

    //Do not compile
    // let mut spaces = "     ";
    // println!("Spaces ->{spaces}<- Spaces");
    // spaces = spaces.len();
    // println!("Number of spaces: {spaces}");
}
